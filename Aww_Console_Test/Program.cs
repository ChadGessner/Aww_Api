using Aww_Api_Fetch;
using Newtonsoft.Json.Linq;
using System.Security.Cryptography.X509Certificates;

Console.WriteLine("Hello, World!");
//ApiHandler handle = new ApiHandler();
//Dictionary<string,string> json = handle.Orchestrate();
//JToken jToken = json["data"]["children"];
//var jArray = jToken.Where(j => (string)j["data"]["post_hint"] == "image");
//jArray.ToList().ForEach(x =>
//{
//    Console.WriteLine(x["data"]["url_overridden_by_dest"]);
//});
//foreach(KeyValuePair<string,string> kvp in json)
//{
//    Console.WriteLine(kvp.Key + "   " + kvp.Value);
//}